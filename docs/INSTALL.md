# Installation

Download the versioned ZIP and its `SHA256SUMS` from the GitHub release. Run
`shasum -a 256 -c SHA256SUMS`, then install the verified ZIP from Wealthfolio’s
add-on settings. Review symbols and mappings before importing.
